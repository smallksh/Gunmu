# 为什么叫滚木

滚木，意为“不存在”。

`null` / `None` / `undefined` 的问题不是它空，
而是它**不告诉你为什么空**。

- 没查到？
- 查到了但是空？
- 出错了？
- 压根没调用？

`gunmu` 用 `Nothing(reason=...)` 把原因带上，
并且通过 `match` 强制你处理两种情况。

滚木不是失败，滚木是“成功这件事本身不存在”。